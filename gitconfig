# vim: syntax=gitconfig

[user]
name = mle
email = maximilian@eul.cc

[core]
autocrlf = input

[color]
ui = auto

[alias]
ann = annotate
annot = annotate
stashes = stash list
remotes = remote -v
tags = tag -l
ci = commit
ciam = commit --amend
shw = show

[push]
default = simple

[credential]
	helper = cache --timeout=3600

[include]
path = .gitconfig-extra

